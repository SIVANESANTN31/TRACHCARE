import 'package:flutter/material.dart';

class patients_list extends StatelessWidget {
  const patients_list({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}